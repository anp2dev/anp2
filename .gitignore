# ANP2 internal-only — never tracked publicly. Everything under internal/
# is private (credentials, memory, research notes, operator-only docs).
internal/

# Belt-and-braces: keep these secret-file globs even if internal/ rule
# above somehow gets edited.
.secrets/
*.pem
*.key
*.crt
*.p12
*.pfx
.env
.env.*
.envrc
secrets.*
credentials.*
*_credentials.*
*_secret.*
*_secrets.*

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# DB
*.db
*.db-journal
*.db-wal
*.db-shm

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/

# MCP / tooling artifacts
.playwright-mcp/

# Local notebook outputs
*.ipynb_checkpoints

# Legacy nested subrepo (remote: anp2dev/anp2 = flagged 2026-05-19).
# Cleanup deferred — see internal/memory/project-ai-net-public-subrepo-stale.md.
# Current discovery-surface source-of-truth is site/ (tracked).
public/

# Claude Code project-config (auto-loaded from repo root; private to operator).
.claude/

# Playwright debugging screenshots (root dir)
*.jpeg
*.playwright-screenshot.png
node_modules/

# Local Python venv for Playwright-driven setup tasks (anp2dev Day-0 work etc.)
.venv-pw/
.playwright-mcp/

# Local-only debugging helpers (CDP debug port exposed, server-side mailbox reader, etc.)
tools/anp2_chrome_launch_cdp.sh
tools/mail_dev_check.sh
# Inbound-actor triage engine — coupled to the internal/ defense policy + signatures
# (themselves gitignored); keep the whole subsystem internal, don't telegraph posture.
tools/inbound_triage.py

# Track the TypeScript SDK build artifacts so the package is publishable
# without operator needing Node/tsc locally.
!prototypes/anp2-client-js/dist/
!prototypes/anp2-client-js/dist/**

# HuggingFace dataset folder is a separate HF git repo (anp2dev/anp2-events).
# Large binary snapshots go to HF via LFS, not into main repo. Metadata (README,
# PUBLISH, .gitattributes, stats.json) is OK to track if explicitly added.
datasets/anp2-events/anp2-events.parquet
datasets/anp2-events/anp2-events.jsonl
CLAUDE.md

# Stray root-level env/ (push.sh historically mis-wrote its log here); internal-only
env/
